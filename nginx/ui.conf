server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # serve /static/** directly (your app/static)
  location /static/ {
    try_files $uri =404;
  }

  # SPA-friendly fallback (keeps / links working)
  location / {
    try_files $uri /index.html;
  }
}

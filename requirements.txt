# --- Core API Framework ---
fastapi
uvicorn[standard]
python-dotenv
python-multipart

# --- Database ---
sqlalchemy
alembic
psycopg[binary]  # Replaces the old psycopg2-binary for modern Python compatibility

# --- Background Tasks & OCR ---
celery
redis
pytesseract
pdf2image
poppler-utils